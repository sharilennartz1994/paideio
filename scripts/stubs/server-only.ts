// Stub per gli script Node fuori dal runtime Next: il pacchetto reale
// `server-only` lancia un'eccezione appena viene caricato fuori da un Server
// Component. Usato solo da `tsconfig.scripts.json`, mai dall'app.
export {};
